DATABASE_URL=postgres://user:password@localhost:5432/frp
API_TOKEN=change-me
PORT=4000
# Comma-separated list of origins the frontend is served from, e.g.
# http://localhost:3000,https://frp.example.com — leave empty to allow any.
CORS_ORIGIN=http://localhost:3000
